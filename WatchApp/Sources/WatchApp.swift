import SwiftUI

/// Placeholder entry point for the JournalInsight watch app.
/// L2 (P-watch) fills in the real glances/complications this wave;
/// this file only keeps the target buildable until then.
@main
struct WatchApp: App {
    var body: some Scene {
        WindowGroup {
            Text("JournalInsight")
        }
    }
}

import SwiftUI

/// B-57 W1 registry hook for "Trends" — L1 stub; lane L2 replaces this body with the full screen.
struct TrendsNativePreview: View {
    var body: some View { TrendsCardNativePreview() }
}
